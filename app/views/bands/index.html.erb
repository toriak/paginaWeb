<h1> Bandas </h1>

<% @bands.each do |band| %>
	<%= link_to band.name, band_path(band) %>
	<br>
<% end %>
<br>
<br>

<%= link_to 'New Bands', new_band_path %>
<br>
<%= link_to 'Profiles', profiles_path %>