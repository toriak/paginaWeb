<div class="jumbotron">

	<h1 align="center"> Instrumentos </h1>

  <div class="container">
  	<div class="well well-lg">
  		<p class="text-center">
  			<% @instruments.each do |intrument| %>
					<%= intrument.name %> <br>
				<% end %>
  		</p>
		</div>
  </div>

  <hr>
  <% if current_user.present? %>
    <%= link_to "Nuevo Instrumentos", new_instrument_path() , :class => "btn btn-primary btn-lg" %>
  <% end %>
</div>